const arr = [1, 2, 3, 4, 5];
console.log(arr[0]); // Accessing the first element
console.log(arr.concat(4));
//console.log(arr.concat([6, 7, 8])); // Concatenating another array
console.log(arr.concat(6, 7, 8)); 